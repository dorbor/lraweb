<div class="col-md-4">
    <p class="text-center">
      <strong>Comments Statistics</strong>
    </p>

    <!-- <div class="progress-group">
      Add Products to Cart
      <span class="float-right"><b>160</b>/200</span>
      <div class="progress progress-sm">
        <div class="progress-bar bg-primary" style="width: 80%"></div>
      </div>
    </div> -->
    <!-- /.progress-group -->
    <!-- /.progress-group -->
    <div class="progress-group">
      <span class="progress-text">Total Comments</span>
      <span class="float-right"><b><%= comments.length %></b></span>
      <div class="progress progress-sm">
        <div class="progress-bar bg-success" style="width: 100%"></div>
      </div>
    </div>

    <div class="progress-group">
      Total Complains
      <span class="float-right"><b><%= complains.length %></b>/<%= comments.length %></span>
      <div class="progress progress-sm">
        <div class="progress-bar bg-danger" style="width: <%= (complains.length/comments.length)*100 %>%"></div>
      </div>
    </div>

    <!-- /.progress-group -->
    <div class="progress-group">
      Total Applauds 
      <span class="float-right"><b><%= applauds.length %></b>/<%= comments.length %></span>
      <div class="progress progress-sm">
        <div class="progress-bar bg-warning" style="width: <%= (applauds.length/comments.length)*100 %>%"></div>
      </div>
    </div>
    <!-- /.progress-group -->
  </div>